# catchment-school — Project Brain

Per-repo brain, migrated from central claude-memory 2026-06-20. Canonical project memory now lives here.

## Current state

- **What:** catchment.school — programmatic SEO site, school catchment-area checker for England. 26,506 school pages (SSR) + ~1,277-1,290 static area/council/guide pages. ~27,795 total URLs.
- **Stack:** Astro 5/6 + Tailwind v4 + `@astrojs/cloudflare` adapter. Schools are SSR (`prerender = false`) served by **Cloudflare Workers** from **D1**; area/council/guide pages are prerendered static. (Site exceeded CF Pages 20k file limit → Workers + D1.)
- **Deploy:** GitHub Actions auto-deploy on push to `master`. Repo `sunnyp81/catchment-school`. CF account `sunnypat81@gmail.com` (ID `aba0a6722a4510842ca473315a8ba13e`), token is GitHub secret `CLOUDFLARE_API_TOKEN`. Deploy cmd inside CI: `cd dist/server && npx wrangler deploy --config wrangler.json`. Canonical local: `C:\Users\sunny\repos\catchment-school` (older notes say `\projects\` or `AppData\Local\Temp\` — both stale).
- **Revenue:** none — **AdSense/monetisation is OFF until ranking recovery.**
- **State (Jun 11):** RECOVERY from May 2026 core-update collapse (see warnings). De-programmatic rebuild in progress (Walsall pilot shipped).
- **Key URLs:** prod catchment.school; staging `catchment-school.sunnypat81.workers.dev`. GA4 `G-E79ZS7MK0H`.
- **D1:** db `catchment-school-db`, ID `e7606fc7-cef5-4996-a9c0-3fc17f707b67`, 26,506 schools. Re-import `node scripts/import-d1.mjs`; schema `scripts/d1-schema.sql`. KV (SESSION) `a47c19e9228849c4a8caeb025c69af75`.
- **Key files:** `src/pages/school/[slug].astro` (SSR, queries D1), `src/pages/area/[slug].astro`, `src/pages/council/[slug].astro`, `src/layouts/Base.astro` (editorial chrome + schema), `src/styles/redesign.css`, `src/data/council-intros.json` (174/178), `src/data/area-intros.json` (~76 towns), `astro.config.mjs` (sitemap filter), `src/pages/sitemap-schools.xml.ts`, `scripts/parse-schools.mjs` + `import-d1.mjs` + `gen-og.mjs`.

## Key facts & warnings

- 🔴 **May 2026 core-update collapse (Jun 11):** sharp break Jun 2 (May 31: 132 clicks → Jun 2: 2). Clean windows: May 14-20 = 1,066 clicks / 29,416 impr / pos 8.62 → Jun 3-9 = 3 clicks / 505 impr / pos 21.95. Clicks **-99.7%**, impr -98.3%. ALL template types hit. Ruled out technical (URL Inspection clean, indexed, robots OK). = **host-level trust/quality classifier flip applied uniformly** — auction suppression, not a ranking slide; no surviving cohort to copy from. Recovery = rebuild DOMAIN trust by making templates materially less programmatic + pruning thin pages. Same failure class as Mar 2026 update killing calculator.place / carehome / radon.tips.
- 🔴 **Recovery plan = de-programmatic, NOT title churn.** STOP broad title rewrites (CHECK-09 bulk-AI-edit risk). Add real per-entity data: oversubscription/fill-rate counts (computable from `schools.json` at build), Ofsted ratings (DfE GIAS, pending mapping), last-distance-admitted (DfE explore-education-statistics CSV — highest-value missing field), council admissions citations, catchment polygons (per-council open data, medium priority). Full rebuild design is in `catchment-rebuild-design-jun11` (central memory) — phased rollout, revert if any phase drops impressions >20% in 7d.
- 🔴 **Cloudflare "Block AI Scrapers and Crawlers" must be toggled OFF** (Dashboard → catchment.school → Security → Bots). CF prepends Disallow blocks for GPTBot/ClaudeBot/PerplexityBot/Google-Extended/CCBot/Bytespider/Amazonbot/meta-externalagent to robots.txt, overriding our allow rules + neutralising llms.txt. Verify: `curl https://catchment.school/robots.txt` should NOT contain `# BEGIN Cloudflare Managed Content`. Gating step for AI-search visibility — open since Apr 28.
- **Thin-page noindex gate:** `/area/` pages serve `noindex, follow` below a school-count threshold. Raised <5 → **<10** on Jun 11 (509/1,096 = 46.4% noindex). Sitemap filter in `astro.config.mjs` excludes noindex pages.
- **GSC property:** `sc-domain:catchment.school` on the **sunnypat81** account (siteOwner). NOT `https://catchment.school/` (old URL-prefix property, unverified, on 2012infinite). Always use the sc-domain form.
- **Sitemap-index must list BOTH** `sitemap-0.xml` (static) and `sitemap-schools.xml` (26,506 school URLs) — they were split, and the index once advertised only the static one (Jun 11 fix).
- 🔴 **Tailwind 4 cascade-layer gotcha:** unlayered CSS (e.g. `a { color: inherit }`) wins over utility classes because TW4 utilities live in `@layer utilities`. Always wrap base resets in `@layer base`.
- **Design preference (Sunny):** white surface + indigo/blue (#2563EB) accent + Inter Tight — he rejected paper-cream/school-green. Default to this for his sites unless he asks for warm/editorial tones.
- **Pending code levers (since Apr 28):** pre-generate `/postcode/{POSTCODE}/` static pages (site captures 0 postcode-search clicks despite the name); catchment polygon GeoJSON overlay (biggest moat); per-page OG variants; pre-render top schools by GSC clicks; self-host fonts/Leaflet; Ofsted + last-distance data enrichment.
- **Credentials:** CF token lives as a GitHub Actions secret / central vault — never inline here.

## History

- **~Mar-Apr 9:** launched. Apr 16 peak 413 clicks / pos 7.6, then Apr 18 honeymoon-bounce to 138 / pos 12.6.
- **Apr 19:** trust-signal deploy — AuthorByline, editorial-policy, Organization schema, per-council intros (top 20), school→hub link mesh. Daily monitor trigger live.
- **Apr 24:** growth sprint — CTR title rewrites all templates, sitemap split (index + static + schools), council intros for 154 more LAs (174/178), 3 guides expanded to 1,800-2,000 words.
- **Apr 28:** health audit (78/100) — found CF AI-bot block, town=LA "Derby, Derby" dedup bug (~4,000 pages, fixed), /postcode opportunity flagged, council data-quality bugs. Title rewrite to lead with school name + Ofsted year.
- **May 9:** GSC audit — explosive growth 0 → 4,378 clicks/28d, pos 8.3. Trailing-slash 301 fix (~3,977 Bing redirects), homepage title/desc, Bing sitemap submitted. Desktop CTR weak (1.4%).
- **May 26:** full redesign — white/indigo-blue/Inter Tight editorial chrome site-wide via Base.astro, llms.txt, Satori og:image, schema hardening, restored `/area/` thin noindex (<5). Deleted temp RedesignBase.astro.
- **Jun 2:** core-update collapse (-99.7%).
- **Jun 11:** collapse diagnosed (host-level quality demotion); de-programmatic rebuild design authored; Phase 0 shipped — sitemap-index fix, noindex gate <5→<10, Walsall pilot (capacity/demand prose + FSM context + official admissions citation + data-derived H2).
- **Jun 27 (deep rebuild):** Sunny escalated alert-utility → **commit to the deep de-programmatic rebuild**. Because the demotion is host-level/uniform (Walsall pilot showed no delta — there's no traffic to measure per-council deltas), rolled the data-driven content across the WHOLE domain in one pass instead of expanding the pilot council-by-council. Shipped + live-verified (`e130f48`, `65c6faa`): (1) **school pages** — un-gated the capacity/demand section from Walsall-only to all 26,506 schools, generalised the prose from hardcoded "Walsall" → each school's own `s.la`, every page now carries a named admissions-source citation (verified URL where known, else council admissions search); a/an grammar fix. (2) **area pages** — per-area demand prose (oversubscription count, faith/academy counts, avg fill rate) + data-derived primary H2; thin gate tightened to also noindex areas with no computable demand data. (3) **council pages** — per-council oversubscription/selective/faith stats sentence. (4) **quality** — purged ALL em/en dashes site-wide (326 in src + ~26k in data files) to ASCII hyphens, killing U+FFFD mojibake that rendered on every school card (ageRange en-dashes); SSR ageRange/name sanitised at render time (`deDash`) so school pages are clean without a D1 re-import. Verified: Cherwell (Oxford) SSR page shows the demand section, no Walsall leak, ageRange "11-18" clean. **Phase 2 (Ofsted) DONE Jun 27 (`519ab62`, live+validated):** joined real DfE Ofsted data (Management Information "latest inspections as at 31 May 2026"; 9,742/26,506 = 36.8% graded — gap is genuine: independents/nurseries/post-Sept-2024 ungraded, correctly omitted, never invented). NOTE: the public GIAS `edubasealldata` CSV does NOT contain Ofsted columns (needs login) — use the Ofsted MI statistical-data-set CSV instead. Activates the already-wired Ofsted badge/sidebar/FAQ/aggregateRating schema on school pages + Ofsted-distribution sentence on area/council. Delivery: bundled compact `src/data/ofsted-map.json` (533KB) into the SSR worker and inject by URN at render time (D1 lacks the field — avoids a re-import); also merged into `schools.json` for static hubs. Validation: Cherwell live = Outstanding badge, 88/100 semantic-audit, 0 dash/mojibake, single H1, no Walsall leak.
**Phase 3 (last-distance) DEFERRED Jun 27 — no national per-URN dataset EXISTS.** Confirmed: DfE explore-education-statistics "applications and offers" is LA-level only (keys old_la_code/new_la_code, NO URN, NO distance indicator); per-school data needs a bespoke request to school.preference@education.gov.uk. It lives only in council admissions booklets (mostly PDFs). A 2-school field = exactly the thin programmatic content that caused the demotion, so NOT shipped. Real source verified for a future per-LA pass: Leeds open data (Datamill North, "Secondary entry" CSVs) — but coverage is sparse (academies leave distance blank) and stale (stops 2021). If pursued, do top-LAs manually, verify each figure against source, never interpolate.
Optional/non-urgent: re-import D1 from cleaned schools.json (`node scripts/import-d1.mjs`, needs CF token) to persist clean ageRange + Ofsted in D1 — currently masked by render-time deDash + bundled ofsted-map, so not needed. **NEXT: measure GSC ~14-21d (look for /school/ + /area/ + /council/ impression recovery); revert any single change if impressions drop >20% in 7d. Remaining moat levers: catchment polygon GeoJSON (per-council open data), pre-generate /postcode/ static pages.**
- **Jul 7:** GSC recovery pass (`1cb2675`, live+verified). Confirmed still no Google recovery (28d = 26 clicks / 2,692 impr, pos 20-55; GA4 organic 2,256 held by Bing). NOT seasonal: site launched Mar 2026 so no YoY data exists; the prior-window "cliff" is just the Jun 2 host demotion boundary. Query data shows the real demand = "[school] catchment area" / "[school] catchment area map" (site ranks 50-165). Fix on school template (all 26,506 SSR pages): added a self-contained, data-derived extractable answer to "What is the catchment area for {school}?" as (1) a highlighted top block in #catchment-area, (2) the FIRST FAQPage schema entry, (3) the first (open) visible FAQ, targeting the money query + AI citation. Also fixed dead Ofsted deep-links `/provider/21/{urn}` -> `search?q={urn}` (3 places). Build clean (26,506 pages), verified live on Parrs Wood (Manchester, real oversubscription data, ofsted search link, 0 broken links). Submitted 5 changed URLs to Bing. No new pages, no title churn, no fabricated stats.
- **Jun 27:** GSC confirms NO recovery 24d post-collapse (0-3 clicks/day, pos ~30-48, still drifting worse; Phase 0 moved nothing). Sunny decision = **pivot to alert utility** (not deep rebuild/park) on the one working signal: the school-page email capture got a real opt-in (The Cherwell School, Oxford) during a ~30-click month. Shipped (`37c5e07`, live+verified): reworked the sidebar "Stay updated" box into a real catchment-alert product — concrete cycle-tied promise ("be first to get the {nextYear} last-distance-admitted figure when {LA} publishes"), capture `la`+`phase` hidden fields for segmented alerts, `#alerts` anchor CTAs inside the oversubscribed/near-capacity warning boxes (peak intent). StaticForms key `sf_9e90…`, subject now includes LA. NEXT (unbuilt): actual alert fulfilment/delivery mechanism + a distribution channel that isn't dead Google (the form has near-zero top-of-funnel until rankings recover).
- **Jul 5 (automated):** `/root/.hermes/scripts/catchment-pilot-control.py` ran its fortnightly Walsall-vs-Sandwell check (due from 25 Jun cadence per `catchment-rebuild-design-jun11.md` Phase 4). Result: **comparison invalidated** — the Jun 27 domain-wide rollout means Sandwell (control) now runs the identical template as Walsall (pilot), so there is no treatment/control split left to measure. Walsall: pos 3.6 / 30 impr / 2 matched URLs over 28d; Sandwell: 0 impr / 0 matched URLs. Fortnightly cron retired — Phase 4 (pilot expansion) is moot, superseded by the Jun 27 sitewide ship. Whole-site GSC still no recovery at that pull: impr 424/7d (-41.2% WoW), clicks 4 (-55.6%), avg pos 28.0. Separately flagged: GA4 sessions +530% WoW (23,878, 97.8% bounce, 8s avg duration) not corroborated by GSC — looks like bot/referral spam, needs Sunny to check traffic source before treating as real growth (unresolved, not actioned here).
- **Jul 8:** Advanced the rebuild design's still-open CHECK-04 gap ("no links between faith schools" — see `catchment-rebuild-design-jun11.md` §1). Phase 3 (last-distance) stays deferred (no national per-URN source exists) and Phase 4 is moot per the Jul 5 finding above, so the next small/safe increment was closing an open checklist item rather than re-running the pilot check. Added a same-religion, same-council cross-link section to every school page with a religious character (`isFaith`): one new D1 query (`laSlug` + `religion` match, LIMIT 4) alongside the existing town-based "nearby" query, rendered as "Other {religion} Schools in {la}" directly under the existing nearby-schools section. Fully computable from fields already in D1 (`religion`, `laSlug`) — no data acquisition, no new pages, template-only change across the existing 26,506 SSR school pages (same CHECK-09-safe pattern as prior phases). Build verified clean; new query/render logic confirmed present in the compiled SSR chunk. Distance-based (lat/lng) nearest-3 cross-links and admissions-type-specific guide links remain open CHECK-04 sub-items for a future step. **NEXT: measure GSC 14-21d post-deploy for /school/ impression movement; if a further >20% 7-day drop correlates with this commit, revert per the design doc's revert trigger.**
- **Jul 11:** Day-14 GSC measurement of the Jun 27 rollout + CHECK-07/Phase-4-remnant increment (`f1e89f7`, live+verified). Measurement: NO revert trigger fired — impressions fell into the Jun 28-Jul 10 window (450 vs 947 prior on /school/) but the decline started Jun 23, four days BEFORE the Jun 27 ship (Jun 18-22 was a transient impressions blip at pos 38-55), so no deploy correlation; meanwhile /school/ avg position IMPROVED 27.6 -> 18.9 post-rollout (weak positive). Clicks still ~0-2/day, no recovery. Ship: extracted the single-entry PILOT_ADMISSIONS_URLS (Walsall) to shared `src/data/admissions-urls.json` and added 22 more councils, each curl-verified live (200 + admissions content) before commit — top-20 GSC councils by impressions plus biggest LAs (lancashire, hertfordshire, essex, etc.), covering ~6,500 school pages + 22 council hubs with a named official citation instead of the Google-search fallback. Hampshire/Kent/Norfolk EXCLUDED — their sites 403 automated fetches so content unverifiable (do-not-fabricate rule); retry manually or from a browser-UA IP later. No new pages, no prose churn, CHECK-09-safe. robots.txt re-checked clean (no CF managed block). **NEXT: GSC re-measure ~Jul 18-25 (day 21-28 of Jun 27 rollout; watch /school/ position trend 27.6->18.9 for continuation); remaining verified-URL gap = Hampshire/Kent/Norfolk; remaining moat levers unchanged (catchment polygons, /postcode/ pages).**
- **Jul 11 (later): overdue Jul 9 pilot-vs-control 28d comparison formally CLOSED.** The Jul 10 pull (window Jun 12-Jul 9, `/root/.hermes/state/catchment-school-pilot-control-2026-07-10.json`) is the due readout: Walsall pilot 0 clicks / 29 impr / pos 3.38 with only 1 matched URL (/area/walsall/); Sandwell control 0 clicks / 0 impr / 0 matched URLs. Full series (Jun 14: pilot 5c/51i/11.9 vs control 0c/37i/12.6 -> Jul 10) shows pilot >= control in every pull, but the arm is uninterpretable: 1-vs-0 matched URLs is no sample, and the Jun 27 sitewide rollout put the pilot template on the control for most of the window. Decision: **rollout stands** (already shipped sitewide Jun 27; nothing in the pilot series or the day-14 whole-site readout — /school/ pos 27.6->18.9, no deploy-correlated >20%/7d drop — argues for reversal); pilot/control framework retired for good (cron already removed Jul 5); **HOLD further school-template changes** until the Jul 18-25 whole-site measurement. No re-pull needed before then.
- **Jul 12:** CHECK-07 continuation, data-only (respects the Jul 11 HOLD - no template or prose touched). Added 16 more live-verified council admissions URLs to `src/data/admissions-urls.json`, next tier by school count: lincolnshire, north-yorkshire, nottinghamshire, suffolk, west-sussex, cambridgeshire, somerset, cornwall, warwickshire, worcestershire, bradford, east-sussex, west-northamptonshire, shropshire, dorset, cheshire-west-and-chester. Each curl-verified (200 + admissions content + correct title/H1) before commit; where a candidate redirected, the working final URL was stored (worcestershire, east-sussex, west-northamptonshire, somerset). Coverage now 39/178 councils: ~4,535 more school pages + 16 council hubs carry a named official citation instead of the Google-search fallback. **Unfetchable from this VPS (403 even with browser UA; WebFetch not permitted in session): Hampshire, Kent, Norfolk (retried per Jul 11 note) plus Leicestershire, Wiltshire, Manchester - all need a real-browser retry. Kirklees skipped: no single admissions landing page (phase-split /beta/admissions/ pages), fallback stays.** robots.txt re-checked clean (no CF managed block). Build clean; new URLs confirmed in SSR chunk + static council pages. NEXT unchanged: GSC whole-site measure Jul 18-25; HOLD on template changes stands.
- **Jul 13:** CHECK-07 continuation, data-only (HOLD respected - no template or prose touched). Added 16 more live-verified council admissions URLs, next tier by school count: cumberland, northumberland, westmorland-and-furness, east-riding-of-yorkshire, wakefield, north-northamptonshire, bolton, wigan, doncaster, leicester, wirral, stockport, salford, central-bedfordshire, sandwell, oldham. Each curl-verified (200 + admissions title/H1/content) before commit; redirect finals stored (cumberland, leicester -> their current apply/admissions landings). **Discovery method that unblocked the 404s: grep the council's `/sitemap.xml` (or the Sitemap line in robots.txt) for "admiss" - search engines (DDG/Bing HTML) block this VPS, but council sitemaps don't.** Coverage now 55/178 councils: +2,360 school pages + 16 council hubs carry a named official citation instead of the Google-search fallback. Still open from the unfetchable list: Hampshire, Kent, Norfolk, Leicestershire (city now DONE - it was the 403'd county), Wiltshire, Manchester (403s, need real-browser retry); Kirklees no single landing. robots.txt clean. Build clean; URLs confirmed in SSR chunk + static council pages. NEXT unchanged: GSC whole-site measure Jul 18-25; HOLD on template changes stands.